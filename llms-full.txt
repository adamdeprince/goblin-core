# Goblin Core — Operator Guide

> Goblin Core is alpha, source-only C++23 server software that speaks the Redis RESP protocol and implements a focused subset centered on sorted sets (ZSETs) and hashes. It stores the same sorted-set data in about 51 bytes per member — roughly half of Redis 7.2.4, and about 35% under modern Redis 8.8 / Valkey 9.1, every engine measured on the same jemalloc 5.3.0 — and serves it faster; hashes get the same lean layout at about 45 bytes per field (roughly half of Redis 7.2.4, and 20–29% under modern Redis 8.8 / Valkey 9.1). This document is a self-contained guide for installing, configuring, and running Goblin Core in production. It intentionally omits code internals; for those, see PERFORMANCE_BRIEF.md.

This guide assumes you already run Redis-style sorted sets (and now hashes) and
want to serve the same data on less RAM. Read it top to bottom to stand up a node;
jump to "Production checklist" when you are ready to deploy.

## What Goblin Core is and is not

Goblin Core is an in-memory, single-node service that speaks the Redis wire
protocol (RESP2) over TCP. Any Redis client library and `redis-cli` talk to it
without changes.

It implements only:

- `PING`
- Sorted-set commands: `ZADD`, `ZCARD`, `ZSCORE`, `ZRANGE`, `ZREVRANGE`,
  `ZRANK`, `ZREVRANK`, `ZREM`
- Hash commands: `HSET`, `HSETNX`, `HGET`, `HMGET`, `HDEL`, `HGETALL`, `HKEYS`,
  `HVALS`, `HLEN`, `HEXISTS`, `HSTRLEN`, `HINCRBY`
- Admin commands: `GOBLIN.MEMORY`, `GOBLIN.OPTIMIZE`, `GOBLIN.SAVE`,
  `GOBLIN.LOAD`

It does NOT implement, and you must not plan around:

- Automatic/background persistence (no scheduled RDB, no AOF per-write log).
  On-demand snapshots exist (`GOBLIN.SAVE`/`GOBLIN.LOAD`; see "Persistence"),
  but between saves data is RAM-only.
- Replication, clustering, or failover.
- Pub/sub, Lua scripting, transactions (`MULTI`/`EXEC`), pipelining semantics
  beyond normal RESP, keyspace notifications.
- Authentication, ACLs, or TLS.
- Eviction/expiry (`EXPIRE`, `maxmemory` policies) — keys live until removed.
- Any key type beyond sorted sets and hashes (no strings, lists, sets, streams).

A key holds at most one type. A hash command against a sorted-set key — or a
sorted-set command against a hash key — returns the standard `WRONGTYPE Operation
against a key holding the wrong kind of value` error rather than coercing the
value, exactly as Redis does with its single-namespace keyspace.

Because saving is on-demand (not per-write) and there is no replication, treat a
Goblin Core node as a snapshot-backed cache/index: snapshot it periodically,
reload the snapshot on startup, and keep a system of record you can rebuild from.

Status: alpha, pre-1.0. Interfaces and defaults may change between releases.
Validate against your own workload before relying on it.

## Requirements

- A C++23 compiler: GCC 14 or newer, or Clang 18 or newer. (Benchmarks are run
  with GCC 16 on a quiet, dedicated 128-core Linux host.)
- CMake 3.25 or newer.
- A 64-bit Linux host is the supported target. Development also builds on macOS
  (arm64), but Linux is what to deploy.
- Redis is only needed if you run the differential tests or comparison
  benchmarks; it is not a runtime dependency.

## Install

Goblin Core is distributed as source. Build it on (or for) the host you will run
it on.

    git clone https://github.com/adamdeprince/goblin-core.git
    cd goblin-core
    git checkout v0.2.0      # pin a release tag
    cmake -S . -B build-release -DCMAKE_BUILD_TYPE=Release
    cmake --build build-release
    ctest --test-dir build-release --output-on-failure   # optional: verify

The server binary is `build-release/goblin-core`. Install it into a prefix if
you want it on PATH:

    cmake --install build-release --prefix /usr/local
    # installs goblin-core to /usr/local/bin

Building with `-DCMAKE_BUILD_TYPE=Release` matters: it enables the optimizer.
Goblin Core relies on the compiler to vectorize hot loops for the host, so build
on hardware similar to where you deploy.

## Run

    goblin-core --bind 0.0.0.0 --port 6379

Defaults when a flag is omitted:

- `--bind 127.0.0.1` — listens on localhost only. Set `--bind 0.0.0.0` (or a
  specific address) to accept remote connections.
- `--port 6379` — the standard Redis port, so existing clients need no change.

The process runs in the foreground and logs to stdout/stderr. It prints the SIMD
features it detected at startup. Run it under a supervisor (systemd, container
orchestrator) in production; see "Running under systemd" below.

## Connect

Any Redis client works. With `redis-cli`:

    redis-cli -p 6379 ZADD leaders 42 alice 17 bob
    redis-cli -p 6379 ZRANGE leaders 0 -1 WITHSCORES
    redis-cli -p 6379 ZSCORE leaders alice
    redis-cli -p 6379 HSET session:42 user alice seen 1719878400
    redis-cli -p 6379 HGETALL session:42

Application clients (redis-py, node-redis, go-redis, Jedis/Lettuce, etc.) connect
the same way; point them at the host and port. Only the commands listed above are
implemented — other commands return an error.

## Commands

Scores are IEEE-754 doubles, exactly as in Redis. Members are arbitrary byte
strings up to 64 KiB each (see "Limits").

- `PING [message]` — liveness check. Returns `PONG`, or echoes `message`.
- `ZADD key score member [score member ...]` — add or update members. Returns
  the number of newly added members. Updating an existing member's score does
  not count.
- `ZCARD key` — number of members in the set.
- `ZSCORE key member` — the member's score, or nil if absent.
- `ZRANGE key start stop [WITHSCORES]` — members by ascending score, by rank
  index. `start`/`stop` are 0-based and may be negative (from the end); `0 -1`
  is the whole set. `WITHSCORES` interleaves scores.
- `ZREVRANGE key start stop [WITHSCORES]` — same, descending.
- `ZRANK key member` — 0-based rank of the member by ascending score, or nil.
- `ZREVRANK key member` — rank by descending score, or nil.
- `ZREM key member [member ...]` — remove members. Returns the number removed.

Hash fields and values are arbitrary byte strings up to 64 KiB each (see
"Limits"). A key holds at most one type: a hash command against a sorted-set key
returns a `WRONGTYPE` error (and vice versa). A hash is unordered, so `HGETALL`,
`HKEYS`, and `HVALS` return fields in an unspecified order.

- `HSET key field value [field value ...]` — set one or more fields. Returns the
  number of fields that were newly created (updating an existing field does not
  count).
- `HSETNX key field value` — set `field` only if it does not already exist.
  Returns `1` if set, `0` if the field was already present.
- `HGET key field` — the field's value, or nil if the field (or key) is absent.
- `HMGET key field [field ...]` — the values for the listed fields, in request
  order, with nil for each missing field.
- `HDEL key field [field ...]` — remove fields. Returns the number removed; the
  key itself is deleted when its last field goes.
- `HGETALL key` — all field/value pairs (field then value, interleaved). Empty
  for a missing key.
- `HKEYS key` — all field names.
- `HVALS key` — all values.
- `HLEN key` — number of fields in the hash.
- `HEXISTS key field` — `1` if the field exists, else `0`.
- `HSTRLEN key field` — byte length of the field's value, or `0` if the field (or
  key) is absent.
- `HINCRBY key field increment` — add the signed integer `increment` to the
  field's value (parsed as a base-10 integer; a missing field starts at `0`) and
  return the new value. Errors if the current value is not an integer or the
  result overflows a 64-bit signed integer.

Admin commands (Goblin Core extensions, not part of Redis):

- `GOBLIN.MEMORY key` — reports per-key allocation, entry count (members or
  fields), and internal index sizing, for a zset or a hash key. Use it to measure
  real bytes-per-member (or bytes-per-field) for capacity planning and to confirm
  the effect of `GOBLIN.OPTIMIZE`.
- `GOBLIN.OPTIMIZE key [density]` — compacts a zset or hash in place and repacks
  its index, returning the number of bytes reclaimed. On a zset it reclaims
  insertion slack; on a hash it drops dead arena bytes left by value updates and
  field deletes and repacks the field index. Run it once after a bulk load,
  before serving reads. `density` is the target member/field-index load factor in
  `(0, 1]` and defaults to `0.97`. Both types also auto-compact on their own (see
  "Tuning: fragmentation and auto-compaction"), so this is an optimization, not a
  requirement. See "Tuning: OPTIMIZE density".
- `GOBLIN.SAVE path [NOACCEL]` — snapshot all zsets and hashes to `path`. Forks a
  copy-on-write child that writes from a frozen image of the data, so it returns
  immediately with `Background saving started` and the server keeps serving;
  completion or failure is logged, and only one background save runs at a time (a
  second returns an error). The child writes to a temp file and renames on
  success, so a crash mid-save never corrupts the previous snapshot. `NOACCEL`
  drops the packed-index accelerator for a smaller, portable file that rebuilds
  indexes on load (see "Persistence").
- `GOBLIN.LOAD path` — replace all current data with the snapshot at `path`.
  Replies with the number of keys loaded. Auto-detects the file by magic: a
  native Goblin snapshot or a Redis `dump.rdb` (see "Migrating from Redis"). On
  any error (bad file, checksum mismatch, unsupported RDB) it fails and leaves
  the store empty.

## Configuration flags

Set these on the command line at startup.

- `--bind ADDRESS` — interface to listen on. Default `127.0.0.1`. Use
  `0.0.0.0` to accept remote connections (only behind trusted network controls).
- `--port PORT` — TCP port. Default `6379`.
- `--load FILE` — load a native snapshot or a Redis `dump.rdb` at startup before
  accepting connections (equivalent to `GOBLIN.LOAD`; the format is auto-
  detected). Logs how many members/keys loaded. If the file is missing or
  invalid the server exits non-zero rather than starting empty. See "Persistence"
  and "Migrating from Redis".
- `--member-index-growth FACTOR` — how much the member hash index grows on each
  rehash, for both the zset member index and the hash field index. Default
  `2^0.25` (about `1.19`). A smaller factor keeps the index fuller (less RAM)
  between rehashes at the cost of more frequent rehashes during heavy writes;
  `2.0` is classic doubling that favors write throughput over steady-state
  memory. The default is memory-first; leave it unless you are write-bound and
  profiling says otherwise.
- `--zset-chunk-bytes BYTES` / `--hash-chunk-bytes BYTES` — size of each
  packed-arena chunk for the respective type. Each must be a power of two and at
  least the per-type minimum — `65536` (64 KiB) for zsets, `131072` (128 KiB) for
  hashes, since one chunk must hold the largest possible entry (a member, or a
  field-plus-value pair). Default `1048576` (1 MiB) for both. Larger chunks reduce
  allocation churn for very large structures; the defaults suit typical workloads.
  An invalid value (not a power of two, or below the minimum) makes the server
  exit with an error.
- `--rank-cache-mode off|exact|block-hint` — optional structure to speed up
  `ZRANK`/`ZREVRANK`. Default `off`. `exact` adds about 4 bytes per member and
  makes rank lookups fast but adds work to every write. `block-hint` is a
  smaller cache that trades some rank speed for lower write cost. Turn this on
  only if `ZRANK` is hot in your workload; it costs memory and write time.
  (`--rank-cache` is a shorthand for `--rank-cache-mode exact`; `--no-rank-cache`
  for `off`.)
- `--score-string-cache` / `--no-score-string-cache` — optional cache of
  RESP-formatted score text for range output. Off by default; it adds per-member
  memory and rarely helps. Leave it off unless a range-heavy benchmark shows a
  win.
- `--max-output-buffer-mib MIB` — per-client cap on queued response bytes before
  the server pauses reading from that socket (backpressure). Default `1`. `0`
  disables the cap (use only for controlled benchmarks). Reads resume after the
  queued output drains below a quarter of the cap.
- `--initial-output-buffer-kib KIB` — per-connection response buffer reserved at
  accept time. Default `0`. Leave it off unless a cold first-burst workload
  justifies the per-connection memory.

## Production checklist

Capacity (RAM):

- Budget about 51 bytes per member after `GOBLIN.OPTIMIZE`. Example: 100M
  members is roughly 4.75 GiB of sorted-set data, plus a small fixed overhead.
- This is about 35% under what Redis 8.8 or Valkey 9.1 spend for the same data,
  and roughly half of older Redis 7.2.4 — so a fixed-RAM box holds about 1.6x as
  much as Redis 8.8, and ~2x as much as Redis 7.2.4.
- Hashes budget about 45 bytes per field after `GOBLIN.OPTIMIZE` (16-byte field +
  16-byte value; a constant ~13.4 bytes of that is per-field overhead — an 8-byte
  struct-of-arrays reference plus an amortized Swiss slot, versus Redis 8.8's
  ~27–38 bytes). Against Redis 7.2.4 that is roughly half the RAM; against modern
  Redis 8.8 it is about 20% leaner, and about 29% leaner than Valkey 9.1. The
  margin grows as values get smaller. Every engine measured on the same jemalloc
  5.3.0 and shared config.
- Measure your real workload with `GOBLIN.MEMORY key` (its reported allocation
  tracks resident memory within a few percent). Member length and set count both
  affect the total.
- Provision headroom: writes temporarily hold extra memory during a rehash or an
  `OPTIMIZE`. Do not run at 100% of RAM.

Load-then-serve pattern (recommended):

1. Start the node.
2. Bulk-load each set with `ZADD` (pipelining is fine and fast).
3. Run `GOBLIN.OPTIMIZE key` on each read-mostly set once loading is done. This
   compacts insertion slack and packs the member index to `0.97` load, giving
   the flat ~49 bytes/member figure and a contiguous, cache-friendly layout for
   reads.
4. Serve reads. Re-run `OPTIMIZE` after a large batch of new writes if you want
   to reclaim slack again.

Durability and restarts:

- Persistence is snapshot-based and on-demand (see "Persistence"). Between saves
  data is RAM-only, so a crash loses writes since the last `GOBLIN.SAVE`. Take
  snapshots on a schedule and before planned shutdowns.
- Restart with `--load SNAPSHOT` to come back with the last saved state instead
  of empty; gate traffic until load finishes (it is synchronous).
- There is no replication or failover. If you need HA, run independent nodes
  each loaded from a snapshot or the source of truth and handle failover in your
  client/proxy.

Security:

- No authentication, ACLs, or TLS. Anyone who can reach the port has full access
  to the data and can run `ZADD`/`ZREM`.
- Bind to a private interface (`--bind` a specific address), restrict with
  firewall/security groups, and terminate TLS and authenticate in a proxy in
  front if you expose it beyond a trusted network.

Running under systemd (example unit):

    [Unit]
    Description=Goblin Core
    After=network-online.target

    [Service]
    ExecStart=/usr/local/bin/goblin-core --bind 0.0.0.0 --port 6379
    Restart=on-failure
    # Goblin Core keeps everything in RAM; size the host and set a limit.
    MemoryMax=8G
    User=goblin
    NoNewPrivileges=true

    [Install]
    WantedBy=multi-user.target

Pair `Restart=on-failure` with `--load` of your latest snapshot (and/or an
application step that repopulates from the source of truth).

Monitoring:

- Health: `PING`.
- Memory per key: `GOBLIN.MEMORY key` (member count, allocated bytes,
  bytes/member, index sizing).
- Process RSS via your normal host metrics; it should track the sum of
  `GOBLIN.MEMORY` allocations within a few percent.

## Persistence

Persistence is snapshot-based and driven by you. By design there is no
append-only write log (AOF) and no internal snapshot scheduler, for two different
reasons. An append-only log adds latency to every write and still forces an
`fsync` interval that can lose data on a crash anyway -- a hot-path cost for
only-partial durability; if you need durable, replayable writes, run them through
a system built for it (Kafka, say) ahead of Goblin Core, which replays them (same
principle as the 64 KiB member cap: do the core operations better than Redis
rather than reimplement a weaker peripheral). Scheduled snapshots are lighter:
`GOBLIN.SAVE` forks and returns immediately, so it costs almost nothing and does
not pause the server; Goblin Core just does not own the policy of when and how
often to run it -- drive that from `cron` or your scheduler.

- `GOBLIN.SAVE path` snapshots every zset and hash to `path`. It forks a copy-on-write
  child that writes from a frozen image of the data, so the command returns
  immediately with `Background saving started` and the server keeps serving while
  the child writes; watch the log for `background save of <path> completed` (or a
  `FAILED` line). Only one background save runs at a time. The child writes to a
  temp file and renames it into place, so a crash mid-save never corrupts the
  previous snapshot.
- `GOBLIN.LOAD path` (or `--load path` at startup) replaces all current data
  with the snapshot. On any error — bad magic, checksum mismatch, truncation —
  it fails and leaves the store empty rather than half-loaded.
- What you lose on a crash: everything written since the last successful
  `GOBLIN.SAVE`. Choose a save cadence that bounds that window, and keep a
  system of record for a full rebuild.

Format and portability: a snapshot has a portable canonical layer (zset members
and scores, hash fields and values, in their own per-type sections) plus a
version-gated accelerator (the packed indexes, dumped so load can skip re-hashing
and re-sorting). Hashes are a newer section, so a current build still loads an
older zset-only snapshot unchanged. The canonical layer is little-endian and
machine-independent, so a snapshot loads on any build or architecture. When the
accelerator cannot be trusted — a newer index format, or a different `std::hash`
(for example moving a file between a macOS/libc++ build and a Linux/libstdc++
build) — the loader ignores it and rebuilds the indexes from canonical: a slower
load, identical data. Checksums are CRC32C.

Accelerator (default) vs canonical-only (`GOBLIN.SAVE path NOACCEL`): the default
accelerated snapshot is the common path. A normal restart memcpy's the packed
indexes back rather than rebuilding them, so at 9.5M members (avx10) it saves in
0.78s (about 5x faster than Redis) and loads in 0.69s (about 5.7x faster than
Redis's 3.98s) -- Goblin runs ~500 MB/s save and ~560 MB/s load versus Redis's
~67 MB/s on both (RDB encode/decode plus skiplist/dict build).

`NOACCEL` is the upgrade/migration path, not a routine-restart option. It drops
the accelerator for a smaller, portable, canonical-only file (294 MB near Redis's
266 MB at 9.5M) whose load rebuilds every index (3.0s -- still faster than Redis,
but well behind the accelerated 0.69s). Reach for it when moving snapshots across
Goblin versions (a changed index format), architectures, or C++ standard
libraries -- cases where a loader would discard the accelerator and rebuild the
indexes anyway, so the smaller file costs nothing extra. For everyday restarts on
the same build, use the default.

Operationally: `GOBLIN.SAVE` before planned shutdowns and on a cron-like
schedule; `--load` the latest snapshot on startup. A snapshot is smaller than
the live RSS (no index slack); `NOACCEL` is roughly the member bytes plus ~10
bytes/member, the default adds the packed indexes on top.

## Migrating from Redis

`--load` and `GOBLIN.LOAD` auto-detect a Redis `dump.rdb` (by its `REDIS` magic)
and import its sorted sets, so migrating an existing zset workload is a one-step
operation:

    goblin-core --port 6379 --load /path/to/dump.rdb
    # or against a running server:
    redis-cli -p 6379 GOBLIN.LOAD /path/to/dump.rdb

- Supported: RDB files from Redis 2.6 through 7.2.x (RDB versions 6-11), all
  encodings of sorted sets (listpack/ziplist/skiplist, binary and text scores,
  integer members).
- Sorted sets are imported; other key types (string/list/set/hash) are parsed
  and skipped. RDB hash import is not yet supported, so a Redis hash does not come
  across this way — move Goblin Core's own hashes between nodes with a native
  `GOBLIN.SAVE` snapshot, or migrate a Redis hash over the wire (`HGETALL` ->
  `HSET`). Streams and modules abort the load with a message — migrate those
  separately (e.g. over the wire).
- `+inf`/`-inf` scores clamp to the largest finite double; a member larger than
  64 KiB aborts the load; a bad CRC64 aborts the load.
- Not supported: Redis 7.4+ (RDB 12+) dumps are rejected. Re-save the dump under
  Redis <= 7.2, or migrate over the wire: `SCAN` for keys, `ZRANGE key 0 -1
  WITHSCORES` (or `ZSCAN`), and `ZADD` into Goblin Core.

After a one-time RDB import, `GOBLIN.SAVE` a native snapshot; later restarts then
use the faster native `--load` (RDB parsing and full index rebuild happen once).

## Tuning: OPTIMIZE density

`GOBLIN.OPTIMIZE key [density]` packs the member index to the given load factor:

- `0.97` (default) — tight, but leaves empty slots so lookups of absent members
  stay fast. Use this for normal read-mostly sets.
- Lower (e.g. `0.90`) — slightly more RAM, marginally faster lookups.
- `1.0` — packs with no empty slots, minimizing RAM. WARNING: with no empty
  slot, a lookup for a member that is NOT present must scan the whole index
  (O(n)). Only use `1.0` for a set that is truly read-only and only ever queried
  for members you know are present. Any miss traffic makes `1.0` far slower.

If in doubt, use the default. Re-running `OPTIMIZE` after writes is safe and
idempotent.

## Tuning: member-index growth

`--member-index-growth` controls steady-state memory for sets and hashes that
receive ongoing writes and are never compacted; it governs both the zset member
index and the hash field index. The default (`2^0.25`) keeps the index at least
~84% full at any size. If you always load-then-`OPTIMIZE` and serve read-mostly,
the growth factor barely matters because `OPTIMIZE` repacks anyway. Raise it
toward `2.0` only if write throughput during bulk load is the bottleneck and you
can spend the extra memory.

## Tuning: arena chunk size

`--zset-chunk-bytes` and `--hash-chunk-bytes` set the packed-arena chunk each
type allocates in (member bytes for a zset; field and value bytes for a hash).
Both default to 1 MiB and must be a power of two no smaller than the per-type
minimum (64 KiB zset, 128 KiB hash — one chunk must hold the largest possible
entry). Leave the defaults unless you run very large structures and want fewer,
larger arena allocations.

## Tuning: fragmentation and auto-compaction

Both sorted sets and hashes keep their variable-length bytes (members, or fields
and values) in a packed arena. Updating a value in place is free when the new
bytes fit; a larger value, or a delete (`ZREM`, `HDEL`), orphans the old bytes as
dead arena space until the structure is rebuilt. To bound that without operator
intervention, each structure auto-compacts: once its dead (reclaimable) arena
bytes exceed its live bytes past a ~1 MiB floor, it rebuilds in place. So a
value-update- or delete-heavy hash (or a `ZREM`-heavy zset) caps its own arena at
roughly twice the live bytes. `GOBLIN.OPTIMIZE` forces the same rebuild
immediately and also repacks the index to your chosen density.

## Limits

- A single member is capped at 64 KiB. This is a deliberate divergence from
  Redis (which allows larger) that keeps per-member overhead small. If your
  members are larger than this, you are using a sorted set as a blob store;
  store the blob elsewhere and keep a key in the set.
- In a hash, each field and each value is capped at 64 KiB as well, for the same
  reason (it keeps the per-field reference to 8 bytes). A value larger than that
  belongs in a blob store — goblin-store.dev — with the hash holding the returned
  key; a hash is an index, not a place to park large blobs.
- Scores are doubles, matching Redis precision.
- Single node, single logical keyspace; no database select semantics beyond the
  default.

## Verifying a build

    ctest --test-dir build-release --output-on-failure

For behavior parity against a real Redis on the supported command subset (needs
a local `redis-server`):

    cmake -S . -B build-redis-tests -DGOBLIN_CORE_REDIS_DIFFERENTIAL_TESTS=ON
    cmake --build build-redis-tests
    ctest --test-dir build-redis-tests --output-on-failure

## Source layout (reference)

You do not need this to operate Goblin Core; it is here for tools that index the
repository. The server is header-oriented under `include/goblin/core/` with entry
point `src/main.cpp`.

- `include/goblin/core/server.hpp` — nonblocking TCP server and RESP I/O loop.
- `include/goblin/core/command.hpp` — command parsing and dispatch.
- `include/goblin/core/store.hpp` — the key store (sorted sets and hashes) and
  `GOBLIN.OPTIMIZE`.
- `include/goblin/core/zset_member_index.hpp` — member-to-id hash index.
- `include/goblin/core/zset_score_index.hpp` — score-ordered index.
- `include/goblin/core/hash.hpp`, `hash_storage.hpp` — the HASH type: Swiss-table
  field index and packed field/value arena.
- `include/goblin/core/snapshot.hpp` — native snapshot format (sections, bytecode,
  CRC32C).
- `include/goblin/core/rdb.hpp`, `src/rdb.cpp` — Redis RDB reader (migration).
- `include/goblin/core/resp_parser.hpp`, `resp_writer.hpp` — RESP protocol.
- `benchmarks/` — Goblin Core vs Redis memory and throughput drivers.
- `tests/redis_differential.py` — Redis-backed behavior parity test.
- `RELEASE.md` — source-release checklist. `BENCHMARKS.md`, `PERFORMANCE_BRIEF.md`
  — measured results and architecture.
